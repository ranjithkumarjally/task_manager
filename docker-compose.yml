
version: "3.8"

services:
  web:
    build: .
    container_name: task_manager_app
    ports:
      - "80:80"
    environment:
      - DATABASE_URL=mysql+pymysql://admin:Ranjith123@my-db.c7aoqyyieltv.eu-north-1.rds.amazonaws.com:3306/taskmanager
      - SECRET_KEY=your-super-secret-key-change-in-production
      - FLASK_ENV=production
      - PORT=80
    restart: unless-stopped

